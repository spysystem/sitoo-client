type: object
description: A resource is an uploaded file.
required:
  - resourceid
properties:
  resourceid:
    type: string
    description: ID for the file as a relative URL (typically the filename).
  filedata:
    type: string
    description: Image as a base64-encoded string